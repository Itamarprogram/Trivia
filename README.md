First You will need to download the tkinter libary and the pygame libary to run the code, <br>
then you will have to download the audio and the images that are linked here as well.

Use: `pip install -r requirements.txt` to install all the library, and <br>
`python Trivia.py` to run the (main) file
